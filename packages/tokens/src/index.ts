/**
 * @continuum/tokens — density and theme token source.
 *
 * core.json, density/{compact,comfortable,tv}.json and the Style
 * Dictionary build (density.css + tokens.ts) land in M1.
 */

export function placeholder(): string {
  return "tokens scaffold — real token curves land in M1";
}
