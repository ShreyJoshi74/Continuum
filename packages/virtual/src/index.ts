/**
 * @continuum/virtual — pure, framework-agnostic window calculator.
 *
 * Returns index ranges only; no DOM measurement in the hot path, which is
 * what keeps CLS structurally near-zero (see M2 exit criteria).
 */

export function placeholder(): string {
  return "virtual window scaffold — real implementation lands in M2";
}
