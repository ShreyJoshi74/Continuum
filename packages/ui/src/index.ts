/**
 * @continuum/ui — cross-surface component library.
 *
 * ~15 components (Card, Rail, Button, Modal, ...) authored focusable-first
 * land in M1. Zero `if (density === 'tv')` branches is the M1 exit bar.
 */

export function placeholder(): string {
  return "ui scaffold — component library lands in M1";
}
