/**
 * @continuum/focus — headless spatial navigation engine.
 *
 * Zero React imports by design: focus geometry is a pure data problem,
 * not a rendering concern, which is also what makes this package
 * publishable standalone (see ADR-1).
 */

export function placeholder(): string {
  return "focus engine scaffold — real implementation lands in M3";
}
